{
	// See https://go.microsoft.com/fwlink/?LinkId=733558 
	// for the documentation about the tasks.json format
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "gcc build active file",
			"command": "make",
			// "args": [
			// 	"-g",
			// 	"${file}",
			// 	"-o",
			// 	"${fileDirname}/${fileBasenameNoExtension}"
			// ],
			"options": {
				"shell": {"executable": "/usr/bin"},
				"cwd": "${workspaceFolder}"
			},
			"problemMatcher": [
				"$gcc"
			],
			"group": "build"
		}
	]
}